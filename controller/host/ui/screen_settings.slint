import { MyWindow } from "elements.slint";

import { NormalText } from "elements.slint";

export component SettingsScreen inherits MyWindow {
    index: 3;

    NormalText {
        text: "Settings Screen!";
    }
}
