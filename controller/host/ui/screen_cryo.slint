import { MyWindow } from "elements.slint";

import { Logic } from "logic.slint";
import { NormalText, StatusOkErr } from "elements.slint";

export component CryoScreen inherits MyWindow {
    index: 1;

    StatusOkErr {
        title: "Water flow";
        ok: Logic.water_flow_ok;
    }
}
